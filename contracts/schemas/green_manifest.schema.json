{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Green Manifest",
  "description": "Metadata for a golf green DTM (Digital Terrain Model)",
  "type": "object",
  "required": ["dtm_id", "course_id", "hole_id", "green_origin_projected_m", "green_rotation_deg"],
  "properties": {
    "dtm_id": {
      "type": "string",
      "description": "Unique identifier for the DTM dataset",
      "pattern": "^[a-z0-9_]+$",
      "example": "riverside_2023_20cm"
    },
    "course_id": {
      "type": "string",
      "description": "Course identifier in lowercase_snake_case",
      "pattern": "^[a-z0-9_]+$",
      "example": "riverside_country_club"
    },
    "hole_id": {
      "type": "integer",
      "description": "Hole number (1-18)",
      "minimum": 1,
      "maximum": 18
    },
    "green_origin_projected_m": {
      "type": "object",
      "description": "Green origin in projected_m coordinates",
      "required": ["x", "y"],
      "properties": {
        "x": {
          "type": "number",
          "description": "X coordinate in meters (eastward)"
        },
        "y": {
          "type": "number",
          "description": "Y coordinate in meters (northward)"
        }
      }
    },
    "green_rotation_deg": {
      "type": "number",
      "description": "Rotation of green relative to projected coordinate system (degrees)",
      "minimum": 0,
      "maximum": 360
    },
    "green_size_m": {
      "type": "object",
      "description": "Green dimensions in meters",
      "properties": {
        "width": {
          "type": "number",
          "description": "Width of green (meters)"
        },
        "depth": {
          "type": "number",
          "description": "Depth of green (meters)"
        }
      }
    },
    "stimp_range": {
      "type": "object",
      "description": "Valid stimp meter range for this green",
      "properties": {
        "min": {
          "type": "number",
          "minimum": 0
        },
        "max": {
          "type": "number",
          "maximum": 20
        }
      }
    }
  }
}

