{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aime.ai/contracts/schemas/solve_putt.request.schema.json",
  "title": "SolvePuttRequest",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "dtm_id": {
      "type": "string",
      "description": "Dataset identifier (allowlisted). Example: riverside_2023_20cm"
    },
    "ball_local_m": { "$ref": "#/$defs/point_local_m" },
    "cup_local_m": { "$ref": "#/$defs/point_local_m" },
    "stimp": {
      "type": "number",
      "minimum": 6.0,
      "maximum": 15.0,
      "description": "Green speed (6.0–15.0)"
    },
    "request_id": {
      "type": "string",
      "description": "Optional tracking ID; if absent, service generates one"
    }
  },
  "required": ["dtm_id", "ball_local_m", "cup_local_m", "stimp"],
  "$defs": {
    "point_local_m": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" }
      },
      "required": ["x", "y"]
    }
  }
}
