# ===========================================
# Agent Alex - Environment Variables Template
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real values to git!

# -------------------------------------------
# Notion Integration (REQUIRED)
# -------------------------------------------
# Get these from https://www.notion.so/my-integrations
NOTION_TOKEN=your_notion_integration_token_here

# Database IDs from your Notion workspace
# Find these in the URL of your Notion databases
NOTION_PROJECTS_DATABASE_ID=your_projects_database_id_here
NOTION_SESSIONS_DATABASE_ID=your_sessions_database_id_here

# -------------------------------------------
# API Configuration
# -------------------------------------------
# Frontend API URL (where your React app makes requests)
# Development: http://localhost:3001
# Production: https://your-app.vercel.app
REACT_APP_API_URL=http://localhost:3001

# -------------------------------------------
# Server Configuration (Optional)
# -------------------------------------------
# Port for the backend server
PORT=3001

# Node environment
# Options: development, production, test
NODE_ENV=development

# -------------------------------------------
# CORS Configuration (Optional)
# -------------------------------------------
# Comma-separated list of allowed origins
# Development default: http://localhost:3000,http://localhost:3001
# Production: Add your Vercel domain
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# -------------------------------------------
# Supabase (Phase D, golf persistence) - replaces in-memory + Notion for golf data
# -------------------------------------------
# Client-safe (shipped to the browser, protected by RLS):
REACT_APP_SUPABASE_URL=https://your-aime-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Server only - bypasses RLS, NEVER put in REACT_APP_* or the client bundle:
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -------------------------------------------
# ElevenLabs caddie voice (Phase D) - replaces OpenAI Realtime
# -------------------------------------------
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_AGENT_ID=your_aime_caddie_agent_id_here
ELEVENLABS_VOICE_ID=your_caddie_voice_id_here

# -------------------------------------------
# OpenAI (LEGACY - retire after the ElevenLabs agent passes a /scenario)
# -------------------------------------------
# WARNING: the current /api/token returns this key to the browser. Rotate it and
# remove that path during the ElevenLabs migration. See docs/INFRA-PHASE-D.md.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# -------------------------------------------
# Weather (golf "plays like" wind/temperature)
# -------------------------------------------
OPEN_WEATHER_API_KEY=your_openweathermap_api_key_here

# -------------------------------------------
# Putt solver (Phase D wiring) - FastAPI :8000 -> DLL service :8081
# -------------------------------------------
REACT_APP_PUTT_API_URL=http://localhost:8000
PUTTSOLVER_SERVICE_URL=http://localhost:8081
# mock | real  (real uses pyproj transforms; needs a registered course DTM)
AIME_TRANSFORM_MODE=mock
# Optional shared secret for POST /api/solve_putt. Leave UNSET for local dev /
# the same-origin app client (endpoint stays open). SET it once the backend is
# deployed + exposed (e.g. as the ElevenLabs voice server tool); callers must
# then send a matching X-AIME-Tool-Secret header. Server-side only.
# SOLVE_PUTT_TOOL_SECRET=

# -------------------------------------------
# Validation
# -------------------------------------------
# Run: node scripts/validate-env.js
# This will check if all required variables are set
