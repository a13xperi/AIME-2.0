# =============================================================================
# Agent Alex Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit your actual .env file to version control!
#
# Run `npm run validate-env` to verify all required variables are set
# =============================================================================

# -----------------------------------------------------------------------------
# Notion API Configuration (Required)
# -----------------------------------------------------------------------------
# Get your API key from: https://www.notion.so/my-integrations
# Create a new integration, then share your databases with it
NOTION_TOKEN=your_notion_integration_token_here

# Get database IDs by opening each database in Notion and copying from the URL
# URL format: notion.so/workspace/DATABASE_ID?v=...
NOTION_PROJECTS_DATABASE_ID=your_projects_database_id_here
NOTION_SESSIONS_DATABASE_ID=your_sessions_database_id_here

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for the Express backend server
PORT=3001

# Environment mode (development, production, test)
NODE_ENV=development

# API URL that the React frontend uses to reach the backend
# Development: http://localhost:3001
# Production: https://your-app.vercel.app
REACT_APP_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------
# Enable debug logging
DEBUG=true

# Log level: error, warn, info, debug
LOG_LEVEL=debug

# CORS allowed origins (comma-separated list)
# In production, set this to your actual domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# -----------------------------------------------------------------------------
# Validation
# -----------------------------------------------------------------------------
# Run: npm run validate-env
# This will check if all required variables are set
